---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: relintdockerhubpushbot/cf-deployment-concourse-tasks
    tag: v4.12.0

inputs:
- name: godog_test

run:
  path: godog_test/task

params:
  VARS:
